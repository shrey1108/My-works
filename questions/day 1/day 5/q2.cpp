#include <bits/stdc++.h>
using namespace std;
int array(vector<int> a){
    int b = a.size();
    vector<int> ans;
    for (int i= 0;i<=b;i++){
     
    
    if(a[i] != i){
       ans.push_back(i);
    }
}
cout<<ans;
}

int main(){

}